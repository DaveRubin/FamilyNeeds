import { TYPES } from '../ResultCard/Docs';

export const results = {
  Health: [
    TYPES.bottles,
    TYPES.breastfeeding,
    TYPES.immunisation,
  ],
  Planning: [
    TYPES.sleeping,
    TYPES.timeforyourself,
  ],
  Caring: [TYPES.pumping],
  Support: [
    TYPES.kindergarten,
    TYPES.kupatCholim,
  ],
};
